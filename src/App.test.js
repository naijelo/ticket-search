import App from './App';

// test('renders learn react link', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/куда/i);
//   expect(linkElement).toBeInTheDocument();
// });
describe("My component", () => {
  test("work", () => {
    expect(1 + 1).toBe(2);
  })
})